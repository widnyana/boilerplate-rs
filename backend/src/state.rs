#[derive(Clone)]
pub struct AppState {
    // db: Database,
    // key: Key,
}

// impl FromRef<AppState> for Key {
//     fn from_ref(state: &AppState) -> Self {
//         state.key.clone()
//     }
// }
